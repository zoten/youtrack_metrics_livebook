You are preparing a concise but insightful weekly delivery report for product and engineering leads.

Use ONLY the the evidence I provide and eventually your recent history chat and chat knowledge.

 * JSON payload below
 * You can add relevant information from any slack channel starting with `#prj-`, from `#team-scrooge-private` or #team-accounting.

Goals:

Summarize what moved on the last working day. Not only as completed delivery, but each relevant change happened in currently active cards.
Summarize weekly progress and risks. Not only as completed delivery, but each relevant change happened in currently active cards.
Highlight cards with notable signals: checklist activity/presence comments indicating progress or uncertainty state transitions special tags: `on hold`, `blocked`, `to be specified`.

Explicitly separate cycle time and net active time interpretation:
 * cycle time starts when the card transitions from inactivity (`todo` or `no state) to an active state
 * cycle time includes on-hold/blocked periods but starts when the card becomes active
 * net active time excludes on-hold/blocked periods
 
Provide two sections:

 * Product-focused summary (scope/value/risk)
 * Engineering-focused summary (flow/coordination/blockers)

both with daily and weekly summary

End with:

 * Top 3 achievements
 * Top 3 risks
 * Recommended next actions for the coming week

Notes:

 * we are using a rotation hint bot on *every* card: unless you find patterns in rotation hints (e.g. all cards with a suggested person are taken by that person, this would signal knowledge silos), that's by design
 * we use 'on hold' tag when waiting for stakeholder or internal team feedback, 'blocked' tag when we're blocked by external teams or external forces



JSON payload:

```
{{{{REPORT_PAYLOAD_JSON}}}}
```
